# API Документация - Fitness App

## Описание

Backend API для фитнес-приложения, построенный на Fastify с использованием TypeScript и Prisma ORM.

**База данных:** PostgreSQL
**Аутентификация:** JWT (Access Token + Refresh Token в cookie)

---

## 📋 Содержание

### 🔐 Аутентификация (`/api/auth`)

- ✅ `POST /signup` - Регистрация пользователя
- ✅ `POST /login` - Вход в систему
- ✅ `POST /refresh` - Обновление токена
- ✅ `POST /logout` - Выход из системы

### 👤 Пользователь (`/api/user`)

- ✅ `GET /me` - Получение информации о себе
- ✅ `PUT /client/profile` - Обновление профиля клиента
- ✅ `PUT /trainer/profile` - Обновление профиля тренера

### 👤 Тренер (`/api/trainer`)

- ✅ `GET /clients` - Получение массива пользователей
- ✅ `PATCH /clients/:clientId/favorite` - Выбрать/убрать пользователя как избранного

### 📊 Прогресс (`/api/progress`)

- ✅ `GET /latest` - Получение последнего отчета
- ✅ `PUT /new-report` - Создание нового отчета
- ✅ `GET /` - Получение всех отчетов клиента
- ✅ `GET /:id` - Получение отчета по ID
- ✅ `POST /:id/comments` - Добавление комментария к отчету (только тренер)

### 📊 Питаниие (`/api/nutrition`)

- ✅ `GET /client/plan` - Получение программы питания конкретного клиента

### 🔄 В разработке

- ⏳ Управление тренировочными программами
- ⏳ Чат между клиентом и тренером
- ⏳ Связь клиент-тренер

---

## Роли пользователей

- `CLIENT` - клиент (пользователь фитнес-приложения)
- `TRAINER` - тренер

---

## 🔐 Аутентификация

Base URL: `/api/auth`

### POST `/signup`

Регистрация нового пользователя (клиента или тренера).

**Query параметры:**

- `role` (optional) - роль пользователя (`CLIENT` | `TRAINER`). По умолчанию: `CLIENT`

**Body (для CLIENT):**

- Тип: `multipart/form-data`
- Обязательные поля:
  - `name` - имя
  - `email` или `phone` - контактные данные
  - `password` - пароль
  - `age` - возраст
  - `photoFront` - фото спереди (обязательно)
  - `photoSide` - фото сбоку (обязательно)
  - `photoBack` - фото сзади (обязательно)
- Опциональные: `goal`, `restrictions`, `experience`, `diet`

**Body (для TRAINER):**

- Тип: `application/json`
- Обязательные поля:
  - `name`, `email` или `phone`, `password`, `age`
- Опциональные: `telegram`, `whatsapp`, `instagram`, `bio`

**Ответ:**

```json
{
  "user": { ...userData },
  "token": {
    "accessToken": "..."
  }
}
```

Refresh token устанавливается в cookie (httpOnly).

---

### POST `/login`

Вход в систему.

**Body:**

```json
{
	"contact": "email@example.com или +79991234567",
	"password": "password123"
}
```

**Ответ:**

```json
{
  "user": { ...userData },
  "token": {
    "accessToken": "..."
  }
}
```

---

### POST `/refresh`

Обновление access токена через refresh token из cookie.

**Headers:**

- Cookie: `refreshToken`

**Ответ:**

```json
{
  "user": { ...userData },
  "token": {
    "accessToken": "..."
  }
}
```

---

### POST `/logout`

Выход из системы (удаление refresh token).

**Headers:**

- `Authorization: Bearer <accessToken>`

**Ответ:**

```json
{
	"message": "Успешный выход"
}
```

---

## 👤 Пользователь

Base URL: `/api/user`

### GET `/me`

Получение информации о текущем пользователе.

**Доступ:** CLIENT, TRAINER
**Headers:**

- `Authorization: Bearer <accessToken>`

**Ответ:**

```json
{
  "user": {
    "id": "...",
    "name": "...",
    "email": "...",
    "phone": "...",
    "photo": "...",
    "age": 25,
    "role": "CLIENT",
    ...
  }
}
```

---

### PUT `/client/profile`

Обновление профиля клиента.

**Доступ:** CLIENT
**Headers:**

- `Authorization: Bearer <accessToken>`

**Body:**

- Тип: `multipart/form-data` (если загружается фото) или `application/json`
- Поля (все опциональные):
  - `name`, `email`, `phone`, `age`
  - `goal`, `restrictions`, `experience`, `diet`
  - `photo` - основное фото профиля (макс. 500KB)

**Ответ:**

```json
{
  "message": "Профиль клиента успешно обновлён",
  "user": { ...updatedUserData }
}
```

---

### PUT `/trainer/profile`

Обновление профиля тренера.

**Доступ:** TRAINER
**Headers:**

- `Authorization: Bearer <accessToken>`

**Body:**

- Тип: `multipart/form-data` (если загружается фото) или `application/json`
- Поля (все опциональные):
  - `name`, `email`, `phone`, `age`
  - `telegram`, `whatsapp`, `instagram`, `bio`
  - `photo` - основное фото профиля (макс. 500KB)

**Ответ:**

```json
{
  "message": "Профиль тренера успешно обновлён",
  "user": { ...updatedUserData }
}
```

---

## 📊 Прогресс

Base URL: `/api/progress`

### GET `/latest`

Получение последнего отчета о прогрессе клиента.

**Доступ:** CLIENT
**Headers:**

- `Authorization: Bearer <accessToken>`

**Ответ:**

```json
{
	"progress": {
		"id": "...",
		"date": "2025-11-29T00:00:00.000Z",
		"weight": 75.5,
		"waist": 80,
		"hips": 95,
		"height": 175,
		"chest": 95,
		"arm": 35,
		"leg": 60,
		"photoFront": "/uploads/photos/...",
		"photoSide": "/uploads/photos/...",
		"photoBack": "/uploads/photos/...",
		"createdAt": "...",
		"updatedAt": "..."
	}
}
```

**Ошибки:**

- `404` - Отчеты о прогрессе не найдены

---

### PUT `/new-report`

Создание нового отчета о прогрессе.

**Доступ:** CLIENT
**Headers:**

- `Authorization: Bearer <accessToken>`

**Body:**

- Тип: `multipart/form-data`
- Обязательные поля:
  - `date` - дата в формате `ДД/ММ/ГГГГ`
  - `weight` - вес (кг)
  - `waist` - обхват талии (см)
  - `hips` - обхват бедер (см)
  - `photoFront` - фото спереди (обязательно)
  - `photoSide` - фото сбоку (обязательно)
  - `photoBack` - фото сзади (обязательно)
- Опциональные поля:
  - `height` - рост (см)
  - `chest` - обхват груди (см)
  - `arm` - обхват руки (см)
  - `leg` - обхват ноги (см)

**Ограничения:**

- Максимальный размер каждого фото: 500KB
- Дата не может быть в будущем
- Нельзя создать два отчета с одинаковой датой

**Ответ:**

```json
{
  "message": "Отчет о прогрессе успешно создан",
  "progress": { ...progressData }
}
```

**Ошибки:**

- `400` - Отчет за эту дату уже существует
- `400` - Отсутствуют обязательные поля
- `400` - Неверный формат даты

---

### GET `/:id`

Получение конкретного отчета о прогрессе по ID.

**Доступ:** CLIENT, TRAINER
**Headers:**

- `Authorization: Bearer <accessToken>`

**Параметры URL:**

- `id` - ID отчета о прогрессе (формат CUID)

**Ответ:**

```json
{
	"progress": {
		"id": "...",
		"userId": "...",
		"date": "2025-11-29T00:00:00.000Z",
		"weight": 75.5,
		"waist": 80,
		"hips": 95,
		"height": 175,
		"chest": 95,
		"arm": 35,
		"leg": 60,
		"photoFront": "/uploads/photos/...",
		"photoSide": "/uploads/photos/...",
		"photoBack": "/uploads/photos/...",
		"createdAt": "...",
		"updatedAt": "...",
		"user": {
			"id": "...",
			"name": "Имя клиента",
			"photo": "/uploads/default/user.png"
		},
		"comments": [
			{
				"id": "...",
				"text": "Отличный прогресс! Продолжайте.",
				"createdAt": "2024-12-03T10:30:00.000Z",
				"trainer": {
					"id": "...",
					"name": "Алексей Смирнов",
					"photo": "/uploads/default/user.png"
				}
			}
		]
	}
}
```

**Права доступа:**

- **CLIENT** - может видеть только свои отчеты
- **TRAINER** - может видеть отчеты всех клиентов (в будущем будет ограничено только своими клиентами)

**Ошибки:**

- `400` - Некорректный формат ID отчета
- `403` - Нет доступа к этому отчету (для клиентов)
- `404` - Отчет о прогрессе не найден

---

### GET `/`

Получение всех отчетов о прогрессе клиента.

**Доступ:** CLIENT
**Headers:**

- `Authorization: Bearer <accessToken>`

**Ответ:**

```json
{
	"progress": [
		{
			"id": "...",
			"date": "2024-11-26T00:00:00.000Z",
			"weight": 74.5,
			"height": 180,
			"chest": 95,
			"waist": 82,
			"hips": 89,
			"arm": 35,
			"leg": 55,
			"photoFront": "/uploads/default/body-fitness.jpg",
			"photoSide": "/uploads/default/body-fitness.jpg",
			"photoBack": "/uploads/default/body-fitness.jpg",
			"createdAt": "...",
			"updatedAt": "..."
		}
		// ... другие отчеты
	]
}
```

**Примечание:** Отчеты отсортированы по дате (от новых к старым).

---

### POST `/:id/comments`

Добавление комментария тренером к отчету о прогрессе.

**Доступ:** TRAINER
**Headers:**

- `Authorization: Bearer <accessToken>`

**Параметры URL:**

- `id` - ID отчета о прогрессе (формат CUID)

**Body:**

```json
{
	"text": "Отлично! Продолжайте в том же духе."
}
```

**Ограничения:**

- Минимальная длина текста: 1 символ
- Максимальная длина текста: 1000 символов

**Ответ:**

```json
{
	"message": "Комментарий успешно добавлен",
	"comment": {
		"id": "...",
		"text": "Отлично! Продолжайте в том же духе.",
		"createdAt": "2024-12-03T10:30:00.000Z",
		"progressId": "...",
		"trainerId": "...",
		"trainer": {
			"id": "...",
			"name": "Алексей Смирнов",
			"photo": "/uploads/default/user.png"
		}
	}
}
```

**Ошибки:**

- `400` - Некорректный формат ID отчета
- `400` - Комментарий не может быть пустым
- `400` - Комментарий не может превышать 1000 символов
- `403` - Доступ только для тренеров
- `404` - Отчет о прогрессе не найден

---

## 📁 Статические файлы

Base URL: `/uploads`

Все загруженные файлы (фотографии профилей, отчетов) доступны по пути `/uploads/...`

Примеры:

- `/uploads/photos/avatar123.jpg`
- `/uploads/default/user.png`

---

## 🔒 Middleware

### authGuard

Проверяет наличие и валидность JWT токена в заголовке `Authorization`.

### hasRole([roles])

Проверяет, что пользователь имеет одну из указанных ролей.

---

## ❌ Обработка ошибок

Все ошибки возвращаются в формате:

```json
{
	"statusCode": 400,
	"error": "Bad Request",
	"message": "Описание ошибки"
}
```

**Коды ошибок:**

- `400` - Bad Request (неверные данные)
- `401` - Unauthorized (не авторизован)
- `403` - Forbidden (нет доступа)
- `404` - Not Found (ресурс не найден)
- `500` - Internal Server Error (ошибка сервера)

---

## 🚀 Статус реализации

- ✅ Аутентификация (регистрация, вход, обновление токена, выход)
- ✅ Управление профилями (клиент, тренер)
- ✅ Создание отчетов о прогрессе
- ✅ Получение последнего отчета о прогрессе
- ✅ Получение конкретного отчета по ID
- ✅ Загрузка и хранение фотографий
- 🔄 В разработке: управление тренировочными программами, чат, комментарии тренера

---

## 📝 Примечания

- Все даты в отчетах хранятся в формате ISO 8601
- Refresh token хранится в httpOnly cookie для безопасности
- Access token должен передаваться в заголовке `Authorization: Bearer <token>`
- При загрузке файлов используется `multipart/form-data`
- Числовые поля в multipart запросах передаются как строки и автоматически конвертируются
