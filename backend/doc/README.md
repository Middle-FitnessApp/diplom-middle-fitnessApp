# 📚 API Документация - Fitness App

## Описание

Backend API для фитнес-приложения, построенный на Fastify с использованием TypeScript и Prisma ORM.

**База данных:** PostgreSQL
**Аутентификация:** JWT (Access Token + Refresh Token в cookie)
**Хранилище файлов:**

- Development: локальная файловая система (`uploads/photos`)
- Production: Supabase Storage

**Структура хранилища:**

```
uploads/photos/
├── users/      ← аватарки пользователей
├── progress/   ← фото прогресса
├── trainers/   ← seed: фото тренеров
└── clients/    ← seed: фото клиентов
```

---

## 📋 Содержание API

### 🔐 Аутентификация

📄 **[Auth API Documentation](./auth.md)**

- ✅ `POST /api/auth/signup` - Регистрация пользователя
- ✅ `POST /api/auth/login` - Вход в систему
- ✅ `POST /api/auth/refresh` - Обновление токена
- ✅ `POST /api/auth/logout` - Выход из системы

---

### 👤 Пользователь

📄 **[User API Documentation](./user.md)**

- ✅ `GET /api/user/me` - Получение информации о себе
- ✅ `PATCH /api/user/client` - Обновление профиля клиента
- ✅ `PATCH /api/user/trainer` - Обновление профиля тренера
- ✅ `POST /api/user/client/invite-trainer` - Отправка приглашения тренеру (CLIENT)
- ✅ `DELETE /api/user/client/invites/:id` - Отмена приглашения тренеру (CLIENT)
- ✅ `DELETE /api/user/client/trainer` - Отмена сотрудничества с тренером (CLIENT)

---

### 👥 Тренер

📄 **[Trainer API Documentation](./trainer.md)**

- ✅ `GET /api/trainer/all` - Получение списка всех тренеров (PUBLIC)
- ✅ `GET /api/trainer/:id` - Получение профиля конкретного тренера (PUBLIC)
- ✅ `GET /api/trainer/invites` - Получение приглашений для тренера (TRAINER)
- ✅ `POST /api/trainer/invites/:id/accept` - Принятие приглашения от клиента (TRAINER)
- ✅ `POST /api/trainer/invites/:id/reject` - Отклонение приглашения от клиента (TRAINER)
- ✅ `GET /api/trainer/clients` - Получение клиентов тренером
- ✅ `PATCH /api/trainer/clients/:id/favorite` - Добавить/убрать клиента в избранное

---

### 📊 Прогресс

📄 **[Progress API Documentation](./progress.md)**

- ✅ `GET /api/progress/latest` - Получение последнего отчета
- ✅ `PUT /api/progress/new-report` - Создание нового отчета
- ✅ `GET /api/progress/` - Получение всех отчетов клиента
- ✅ `GET /api/progress/:id` - Получение отчета по ID
- ✅ `GET /api/progress/:id/comments` - Получение комментариев к отчету
- ✅ `POST /api/progress/:id/comments` - Добавление комментария (TRAINER)

---

### 🍽️ Питание

📄 **[Nutrition API Documentation](./nutrition.md)**

**Клиент:**

- ✅ `GET /api/nutrition/client/plan` - Получение плана питания клиента (поддержка циклов 7/30 дней)
- ✅ `GET /api/nutrition/client/history` - История назначенных планов питания

**Тренер - Категории:**

- ✅ `POST /api/nutrition/categories` - Создание категории
- ✅ `GET /api/nutrition/categories` - Получение категорий с подкатегориями
- ✅ `PUT /api/nutrition/categories/:id` - Обновление категории
- ✅ `DELETE /api/nutrition/categories/:id` - Удаление категории

**Тренер - Подкатегории:**

- ✅ `POST /api/nutrition/categories/:id/subcategories` - Создание подкатегории
- ✅ `GET /api/nutrition/categories/:id/subcategories` - Получение подкатегорий
- ✅ `PUT /api/nutrition/subcategories/:id` - Обновление подкатегории
- ✅ `DELETE /api/nutrition/subcategories/:id` - Удаление подкатегории

---

### 🔄 В разработке

- ⏳ CRUD для дней и приемов пищи (NutritionDay, NutritionMeal)
- ⏳ Управление тренировочными программами
- ⏳ Чат между клиентом и тренером

---

## 🗄️ База данных

### Seed-данные для тестирования

Проект включает seed-скрипт для быстрого заполнения БД тестовыми данными:

```bash
npm run prisma:seed
```

**Что создаётся:**

- 20 тренеров с фото и профилями
- 100 клиентов с фото и профилями
- ~500 прогресс-отчётов с 3 фото каждый
- Связи тренер-клиент (PENDING/ACCEPTED)
- Избранные клиенты у тренеров
- Планы питания на 30 дней с циклами
- Комментарии к прогрессам

**Тестовые аккаунты:**

- Клиенты: `client1@mail.ru` до `client100@mail.ru`
- Тренеры: `trainer1@mail.ru` до `trainer20@mail.ru`
- Пароль для всех: `123456`

---

## 🏗️ Общая информация

### Технологии

- **Framework:** Fastify 5.6.2
- **Database:** PostgreSQL + Prisma ORM 6.19.0
- **Validation:** Zod 4.1.12
- **Authentication:** JWT (Access + Refresh tokens)
- **File Storage:** Local (dev) / Supabase Storage (prod)

### Роли пользователей

- **CLIENT** - клиент (пользователь фитнес-приложения)
- **TRAINER** - тренер

### Базовые правила

**Аутентификация:**

- Access token (15 мин) передается в заголовке: `Authorization: Bearer <token>`
- Refresh token (30 дней) хранится в httpOnly cookie

**Форматы данных:**

- Даты: ISO 8601 (`2025-12-04T10:30:00.000Z`)
- Загрузка файлов: `multipart/form-data`
- Максимальный размер фото: 500KB

**Обработка ошибок:**
Все ошибки возвращаются в формате:

```json
{
	"statusCode": 400,
	"error": "Bad Request",
	"message": "Описание ошибки"
}
```

**Коды статусов:**

- `200` OK, `201` Created
- `400` Bad Request, `401` Unauthorized, `403` Forbidden, `404` Not Found
- `500` Internal Server Error

---

## 🔄 Последние обновления

### v1.2.0 - Декабрь 2025

**Структура хранилища файлов:**

- ✅ Организация фото по подпапкам (`users/`, `progress/`, `trainers/`, `clients/`)
- ✅ Поддержка подпапок в Supabase Storage
- ✅ Автоматическое создание директорий

**API изменения:**

- ✅ `PATCH /api/user/client` - обновление профиля клиента (было `PUT /api/user/client/profile`)
- ✅ `PATCH /api/user/trainer` - обновление профиля тренера (было `PUT /api/user/trainer/profile`)
- ✅ `GET /api/nutrition/client/history` - история планов питания

\*\*Функциональность:

- ✅ Циклические планы питания (автоматический расчёт дней)
- ✅ Система приглашений тренер-клиент
- ✅ Избранные клиенты у тренера
- ✅ Комментарии к прогрессу
