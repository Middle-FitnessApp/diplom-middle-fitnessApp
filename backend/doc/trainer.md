# 👥 Trainer API (Тренер)

Base URL: `/api/trainer`

---

## GET `/all`

Получение списка всех тренеров (публичный эндпоинт).

**Доступ:** PUBLIC (без авторизации)

**Ответ:**

```json
{
	"trainers": [
		{
			"id": "clx123...",
			"name": "Алексей Смирнов",
			"photo": "/uploads/default/user.png",
			"bio": "Опытный тренер с 10-летним стажем...",
			"telegram": "@alex_trainer",
			"whatsapp": "+79991234567",
			"instagram": "@alex_fitness"
		},
		{
			"id": "clx456...",
			"name": "Елена Кузнецова",
			"photo": "/uploads/photos/trainer2.jpg",
			"bio": "Специалист по функциональным тренировкам...",
			"telegram": null,
			"whatsapp": "+79992222222",
			"instagram": "@elena_fit"
		}
	]
}
```

**Примечание:** Тренеры отсортированы по имени (A-Z).

---

## GET `/clients`

Получение списка клиентов, которые выбрали этого тренера.

**Доступ:** TRAINER

**Headers:**

- `Authorization: Bearer <accessToken>`

**Ответ:**

```json
{
	"clients": [
		{
			"id": "...",
			"email": "client1@mail.ru",
			"name": "Иван Иванов",
			"age": 28,
			"phone": null,
			"photo": "/uploads/default/user.png",
			"role": "CLIENT",
			"isFavorite": true
		},
		{
			"id": "...",
			"email": null,
			"name": "Мария Петрова",
			"age": 25,
			"phone": "+79161234567",
			"photo": "/uploads/photos/client2.jpg",
			"role": "CLIENT",
			"isFavorite": false
		}
	]
}
```

**Примечание:**

- Возвращаются только клиенты, которые выбрали этого тренера
- `isFavorite: true` - клиент в избранном (в работе)
- `isFavorite: false` - клиент пока не в избранном

---

## PATCH `/clients/:clientId/favorite`

Добавить/убрать клиента в избранное (взять в работу).

**Доступ:** TRAINER

**Headers:**

- `Authorization: Bearer <accessToken>`

**Параметры URL:**

- `clientId` - ID клиента (формат CUID)

**Ответ:**

```json
{
	"isFavorite": true
}
```

**Ошибки:**

- `400` - Некорректный формат ID клиента
- `403` - Доступ только для тренеров
- `404` - Клиент не найден или не выбрал этого тренера

**Примечание:**

- Toggle действие: если клиент в избранном → убрать, если нет → добавить
- Создаёт связь TrainerClient со статусом ACCEPTED при первом добавлении

---

## 📝 Модель связи тренер-клиент

### Структура TrainerClient:

```typescript
{
  id: string           // CUID
  trainerId: string    // ID тренера
  clientId: string     // ID клиента
  status: 'PENDING' | 'ACCEPTED' | 'REJECTED'  // Статус приглашения
  isFavorite: boolean  // Клиент в избранном (в работе)
  createdAt: Date      // Дата создания связи
  acceptedAt: Date?    // Когда тренер принял приглашение
}
```

### Статусы:

- **PENDING** - Клиент отправил приглашение, ждёт ответа
- **ACCEPTED** - Тренер принял, работает с клиентом
- **REJECTED** - Тренер отклонил приглашение

### Примечание:

- Один клиент может пригласить нескольких тренеров
- Первый тренер, принявший приглашение, становится активным
- Остальные приглашения автоматически отклоняются
