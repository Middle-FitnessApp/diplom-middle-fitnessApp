# Nutrition API - Документация

API для работы с планами питания, категориями и подкатегориями.

**Base URL:** `/api/nutrition`

---

## Оглавление

- [Получение плана питания клиента](#получение-плана-питания-клиента)
- [Создание категории питания](#создание-категории-питания)
- [Получение категорий тренера](#получение-категорий-тренера)
- [Обновление категории](#обновление-категории)
- [Удаление категории](#удаление-категории)
- [Создание подкатегории](#создание-подкатегории)
- [Получение подкатегорий](#получение-подкатегорий)
- [Обновление подкатегории](#обновление-подкатегории)
- [Удаление подкатегории](#удаление-подкатегории)
- [Создание плана на день](#создание-плана-на-день)
- [Получение планов на день](#получение-планов-на-день)
- [Просмотр конкретного плана](#просмотр-конкретного-плана)
- [Редактирование плана](#редактирование-плана)
- [Удаление плана](#удаление-плана)
- [Модели данных](#модели-данных)

---

## Получение плана питания клиента

Получает назначенный активный план питания для текущего клиента с циклическим расчётом дней. Доступно только клиентам с активным тренером. Для тренеров доступен план только принятых клиентов.

**Endpoint:** `GET /api/nutrition/client/plan`

**Права доступа:** `CLIENT` (только с активным тренером), `TRAINER` (только для принятых клиентов)

**Headers:**

```
Authorization: Bearer <access_token>
```

### Query параметры

| Параметр   | Тип      | Обязательный                   | По умолчанию | Описание                                  |
| ---------- | -------- | ------------------------------ | ------------ | ----------------------------------------- |
| `clientId` | `string` | Да для TRAINER, нет для CLIENT | -            | ID клиента (только для тренера)           |
| `period`   | `string` | Нет                            | `day`        | Период фильтрации: `day`, `week`, `month` |
| `date`     | `string` | Нет                            | сегодня      | Целевая дата в формате `YYYY-MM-DD`       |

#### Описание параметров:

- **`clientId`**: Обязателен для роли `TRAINER`. Указывает ID клиента, чей план нужно получить. Доступ разрешен только если клиент принят тренером.

- **`period`**:

  - `day` — показывает 1 день (текущий день цикла)
  - `week` — показывает 7 дней начиная с текущего
  - `month` — показывает до 31 дня начиная с текущего

- **`date`** — дата, относительно которой рассчитывается позиция в цикле. Если не указана, используется сегодняшняя дата.

### Логика циклического расчёта

План питания работает по **линейному циклу**:

1. Тренер назначает план с N днями (например, 3 дня, 7 дней, 10 дней)
2. План начинается с `startDate` (дата назначения)
3. Дни циклически повторяются: День 1 → День 2 → ... → День N → День 1 → ...
4. Система автоматически вычисляет, какой день актуален сегодня

**Пример:** План из 3 дней, назначен 1 декабря:

- 1 декабря → День 1
- 2 декабря → День 2
- 3 декабря → День 3
- 4 декабря → День 1
- 5 декабря → День 2
- и так далее...

### Успешный ответ (200 OK)

#### Пример 1: Запрос текущего дня (period=day)

```http
GET /api/nutrition/client/plan?period=day
```

```json
{
	"plan": {
		"id": "cm4xyz123",
		"subcategory": {
			"id": "cm4abc456",
			"name": "Набор массы",
			"description": "План для набора мышечной массы"
		},
		"startDate": "2025-12-01",
		"assignedAt": "2025-12-01T10:00:00.000Z"
	},
	"days": [
		{
			"id": "cm4day001",
			"subcatId": "cm4abc456",
			"dayTitle": "День 2",
			"dayOrder": 2,
			"date": "2025-12-06",
			"isToday": true,
			"createdAt": "2024-12-01T10:00:00.000Z",
			"updatedAt": "2024-12-01T10:00:00.000Z",
			"meals": [
				{
					"id": "cm4meal001",
					"dayId": "cm4day001",
					"type": "BREAKFAST",
					"name": "Завтрак",
					"mealOrder": 1,
					"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"],
					"createdAt": "2024-12-01T10:00:00.000Z",
					"updatedAt": "2024-12-01T10:00:00.000Z"
				},
				{
					"id": "cm4meal002",
					"dayId": "cm4day001",
					"type": "LUNCH",
					"name": "Обед",
					"mealOrder": 2,
					"items": ["Куриная грудка", "Рис", "Салат"],
					"createdAt": "2024-12-01T10:00:00.000Z",
					"updatedAt": "2024-12-01T10:00:00.000Z"
				}
			]
		}
	]
}
```

#### Пример 2: Запрос недели (period=week)

```http
GET /api/nutrition/client/plan?period=week
```

```json
{
	"plan": {
		"id": "cm4xyz123",
		"subcategory": {
			"id": "cm4abc456",
			"name": "Набор массы",
			"description": "План для набора мышечной массы"
		},
		"startDate": "2025-12-01",
		"assignedAt": "2025-12-01T10:00:00.000Z"
	},
	"days": [
		{
			"id": "cm4day002",
			"dayTitle": "День 2",
			"dayOrder": 2,
			"date": "2025-12-06",
			"isToday": true,
			"meals": [...]
		},
		{
			"id": "cm4day003",
			"dayTitle": "День 3",
			"dayOrder": 3,
			"date": "2025-12-07",
			"isToday": false,
			"meals": [...]
		},
		{
			"id": "cm4day001",
			"dayTitle": "День 1",
			"dayOrder": 1,
			"date": "2025-12-08",
			"isToday": false,
			"meals": [...]
		}
		// ... еще 4 дня (всего 7)
	]
}
```

#### Пример 3: План из 3 дней на месяц (period=month)

Если план содержит 3 дня, при запросе `period=month` система вернёт только 3 дня (не дублирует весь цикл):

```http
GET /api/nutrition/client/plan?period=month
```

```json
{
	"plan": {...},
	"days": [
		// 3 дня в порядке от текущего дня цикла
	]
}
```

#### Пример 4: Запрос с конкретной датой

```http
GET /api/nutrition/client/plan?period=day&date=2025-12-10
```

Вернёт день, который актуален для 10 декабря согласно циклу.

#### Пример 3: Запрос тренером плана клиента (period=day)

```http
GET /api/nutrition/client/plan?clientId=cm4client123&period=day
```

```json
{
	"plan": {
		"id": "cm4xyz123",
		"subcategory": {
			"id": "cm4abc456",
			"name": "Набор массы",
			"description": "План для набора мышечной массы"
		},
		"startDate": "2025-12-01",
		"assignedAt": "2025-12-01T10:00:00.000Z"
	},
	"days": [
		{
			"id": "cm4day001",
			"subcatId": "cm4abc456",
			"dayTitle": "День 2",
			"dayOrder": 2,
			"date": "2025-12-06",
			"isToday": true,
			"createdAt": "2024-12-01T10:00:00.000Z",
			"updatedAt": "2024-12-01T10:00:00.000Z",
			"meals": [
				{
					"id": "cm4meal001",
					"dayId": "cm4day001",
					"type": "BREAKFAST",
					"name": "Завтрак",
					"mealOrder": 1,
					"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"],
					"createdAt": "2024-12-01T10:00:00.000Z",
					"updatedAt": "2024-12-01T10:00:00.000Z"
				}
			]
		}
	]
}
```

### Ответ при отсутствии плана (200 OK)

```json
{
	"plan": null,
	"days": []
}
```

### Описание полей ответа

**Plan:**

- `id` - ID назначения плана
- `subcategory` - Информация о подкатегории (программе)
- `startDate` - Дата начала плана (для расчёта цикла)
- `assignedAt` - Дата назначения плана

**Day:**

- `id` - ID дня
- `subcatId` - ID подкатегории
- `dayTitle` - Название дня
- `dayOrder` - Порядковый номер дня в плане
- `date` - Рассчитанная дата для этого дня (формат YYYY-MM-DD)
- `isToday` - Флаг текущего дня (true только для первого дня в списке)
- `meals` - Массив приёмов пищи
- `createdAt`, `updatedAt` - Временные метки

**Meal:**

- `id` - ID приёма пищи
- `dayId` - ID дня
- `type` - Тип приёма (`BREAKFAST`, `SNACK`, `LUNCH`, `DINNER`)
- `name` - Название приёма
- `mealOrder` - Порядковый номер приёма
- `items` - Массив блюд/продуктов
- `createdAt`, `updatedAt` - Временные метки

### Ошибки

- **400 Bad Request** - Параметр `clientId` обязателен для тренера, или `clientId` не разрешен для клиента
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Недостаточно прав доступа (клиент не принят тренером, или роль не поддерживается)
- **403 Forbidden** - Доступ запрещен: У вас нет активного тренера

---

## История планов питания клиента

Получает архив завершённых/замененных планов питания для текущего клиента.

**Endpoint:** `GET /api/nutrition/client/history`

**Права доступа:** `CLIENT`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Query параметры

| Параметр | Тип      | Обязательный | По умолчанию | Описание                                 |
| -------- | -------- | ------------ | ------------ | ---------------------------------------- |
| `page`   | `number` | Нет          | `1`          | Номер страницы (начиная с 1)             |
| `limit`  | `number` | Нет          | `10`         | Количество элементов на странице (1-100) |

### Когда план попадает в историю

План автоматически переходит в историю (`isActive: false`) когда:

- Тренер назначает клиенту новый план питания
- Старый активный план заменяется новым

У клиента может быть только **один активный план** в любой момент времени.

### Успешный ответ (200 OK)

#### Пример запроса

```http
GET /api/nutrition/client/history?page=1&limit=10
```

#### Пример ответа

```json
{
	"history": [
		{
			"id": "cm4plan001",
			"categoryName": "Набор массы",
			"subcategoryName": "Для мужчин 70-80кг",
			"startDate": "2025-11-01T00:00:00.000Z",
			"assignedAt": "2025-11-01T10:00:00.000Z",
			"replacedAt": "2025-12-01T10:00:00.000Z"
		},
		{
			"id": "cm4plan002",
			"categoryName": "Похудение",
			"subcategoryName": "Стандартный",
			"startDate": "2025-10-01T00:00:00.000Z",
			"assignedAt": "2025-10-01T09:00:00.000Z",
			"replacedAt": "2025-11-01T10:00:00.000Z"
		}
	],
	"pagination": {
		"page": 1,
		"limit": 10,
		"total": 2,
		"totalPages": 1
	}
}
```

### Описание полей ответа

**History item:**

- `id` - ID назначения плана
- `categoryName` - Название категории плана
- `subcategoryName` - Название подкатегории (программы)
- `startDate` - Дата начала действия плана
- `assignedAt` - Когда план был назначен
- `replacedAt` - Когда план был заменён новым (дата обновления)

**Pagination:**

- `page` - Текущая страница
- `limit` - Элементов на странице
- `total` - Всего элементов в истории
- `totalPages` - Всего страниц

### Пустая история (200 OK)

```json
{
	"history": [],
	"pagination": {
		"page": 1,
		"limit": 10,
		"total": 0,
		"totalPages": 0
	}
}
```

### Ошибки

- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для клиентов
- **400 Bad Request** - Некорректные параметры пагинации

---

## Создание категории питания

Создает новую глобальную категорию питания для тренера.

**Endpoint:** `POST /api/nutrition/categories`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Тело запроса

```json
{
	"name": "Набор массы",
	"description": "Программы питания для набора мышечной массы"
}
```

| Поле        | Тип    | Обязательно | Описание                        |
| ----------- | ------ | ----------- | ------------------------------- |
| name        | string | Да          | Название категории (уникальное) |
| description | string | Нет         | Описание категории              |

### Успешный ответ (201 Created)

```json
{
	"id": "cm4cat123",
	"trainerId": "cm4trainer456",
	"name": "Набор массы",
	"description": "Программы питания для набора мышечной массы",
	"createdAt": "2024-12-05T10:00:00.000Z",
	"updatedAt": "2024-12-05T10:00:00.000Z"
}
```

### Ошибки

- **400 Bad Request** - Категория с таким названием уже существует
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Получение категорий тренера

Получает все категории питания текущего тренера со всеми подкатегориями.

**Endpoint:** `GET /api/nutrition/categories`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Успешный ответ (200 OK)

```json
[
	{
		"id": "cm4cat123",
		"trainerId": "cm4trainer456",
		"name": "Набор массы",
		"description": "Программы питания для набора мышечной массы",
		"createdAt": "2024-12-05T10:00:00.000Z",
		"updatedAt": "2024-12-05T10:00:00.000Z",
		"subcategories": [
			{
				"id": "cm4sub001",
				"categoryId": "cm4cat123",
				"name": "Базовая программа",
				"description": "Для начинающих",
				"createdAt": "2024-12-05T11:00:00.000Z",
				"updatedAt": "2024-12-05T11:00:00.000Z"
			}
		]
	}
]
```

### Ошибки

- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Обновление категории

Обновляет существующую категорию питания тренера.

**Endpoint:** `PUT /api/nutrition/categories/:id`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Параметры URL

| Параметр | Тип    | Описание     |
| -------- | ------ | ------------ |
| id       | string | ID категории |

### Тело запроса

```json
{
	"name": "Набор массы (обновлено)",
	"description": "Обновленное описание"
}
```

Все поля опциональны.

### Успешный ответ (200 OK)

```json
{
	"id": "cm4cat123",
	"trainerId": "cm4trainer456",
	"name": "Набор массы (обновлено)",
	"description": "Обновленное описание",
	"createdAt": "2024-12-05T10:00:00.000Z",
	"updatedAt": "2024-12-05T12:00:00.000Z"
}
```

### Ошибки

- **400 Bad Request** - Категория с таким названием уже существует
- **404 Not Found** - Категория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Удаление категории

Удаляет категорию питания тренера. Каскадно удаляет все подкатегории, дни и приемы пищи.

**Endpoint:** `DELETE /api/nutrition/categories/:id`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Параметры URL

| Параметр | Тип    | Описание     |
| -------- | ------ | ------------ |
| id       | string | ID категории |

### Успешный ответ (204 No Content)

Пустое тело ответа.

### Ошибки

- **404 Not Found** - Категория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Создание подкатегории

Создает новую подкатегорию (программу питания) внутри категории.

**Endpoint:** `POST /api/nutrition/categories/:id/subcategories`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Параметры URL

| Параметр | Тип    | Описание     |
| -------- | ------ | ------------ |
| id       | string | ID категории |

### Тело запроса

```json
{
	"name": "Программа для начинающих",
	"description": "Базовый план питания на неделю"
}
```

| Поле        | Тип    | Обязательно | Описание                           |
| ----------- | ------ | ----------- | ---------------------------------- |
| name        | string | Да          | Название подкатегории (уникальное) |
| description | string | Нет         | Описание подкатегории              |

### Успешный ответ (201 Created)

```json
{
	"id": "cm4sub001",
	"categoryId": "cm4cat123",
	"name": "Программа для начинающих",
	"description": "Базовый план питания на неделю",
	"createdAt": "2024-12-05T11:00:00.000Z",
	"updatedAt": "2024-12-05T11:00:00.000Z"
}
```

### Ошибки

- **400 Bad Request** - Подкатегория с таким названием уже существует
- **404 Not Found** - Категория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Получение подкатегорий

Получает все подкатегории конкретной категории.

**Endpoint:** `GET /api/nutrition/categories/:id/subcategories`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Параметры URL

| Параметр | Тип    | Описание     |
| -------- | ------ | ------------ |
| id       | string | ID категории |

### Успешный ответ (200 OK)

```json
[
	{
		"id": "cm4sub001",
		"categoryId": "cm4cat123",
		"name": "Программа для начинающих",
		"description": "Базовый план питания на неделю",
		"createdAt": "2024-12-05T11:00:00.000Z",
		"updatedAt": "2024-12-05T11:00:00.000Z"
	},
	{
		"id": "cm4sub002",
		"categoryId": "cm4cat123",
		"name": "Продвинутая программа",
		"description": "Для опытных спортсменов",
		"createdAt": "2024-12-05T12:00:00.000Z",
		"updatedAt": "2024-12-05T12:00:00.000Z"
	}
]
```

### Ошибки

- **404 Not Found** - Категория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Обновление подкатегории

Обновляет существующую подкатегорию.

**Endpoint:** `PUT /api/nutrition/subcategories/:id`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Параметры URL

| Параметр | Тип    | Описание        |
| -------- | ------ | --------------- |
| id       | string | ID подкатегории |

### Тело запроса

```json
{
	"name": "Программа для начинающих (обновлено)",
	"description": "Обновленное описание"
}
```

Все поля опциональны.

### Успешный ответ (200 OK)

```json
{
	"id": "cm4sub001",
	"categoryId": "cm4cat123",
	"name": "Программа для начинающих (обновлено)",
	"description": "Обновленное описание",
	"createdAt": "2024-12-05T11:00:00.000Z",
	"updatedAt": "2024-12-05T13:00:00.000Z"
}
```

### Ошибки

- **400 Bad Request** - Подкатегория с таким названием уже существует
- **404 Not Found** - Подкатегория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Удаление подкатегории

Удаляет подкатегорию. Каскадно удаляет все дни и приемы пищи.

**Endpoint:** `DELETE /api/nutrition/subcategories/:id`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Параметры URL

| Параметр | Тип    | Описание        |
| -------- | ------ | --------------- |
| id       | string | ID подкатегории |

### Успешный ответ (204 No Content)

Пустое тело ответа.

### Ошибки

- **404 Not Found** - Подкатегория не найдена или нет прав доступа
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров

---

## Создание плана на день

Создаёт новый план питания на день в указанной подкатегории.

**Endpoint:** `POST /api/nutrition/subcategories/{subcategoryId}/days`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Тело запроса

```json
{
	"dayTitle": "День 1: Набор массы",
	"dayOrder": 1,
	"meals": [
		{
			"type": "BREAKFAST",
			"name": "Завтрак",
			"mealOrder": 1,
			"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"]
		},
		{
			"type": "LUNCH",
			"name": "Обед",
			"mealOrder": 2,
			"items": ["Куриная грудка", "Рис", "Салат"]
		}
	]
}
```

### Успешный ответ (201 Created)

```json
{
	"id": "cm4day001",
	"subcatId": "cm4sub001",
	"dayTitle": "День 1: Набор массы",
	"dayOrder": 1,
	"createdAt": "2025-12-09T10:00:00.000Z",
	"updatedAt": "2025-12-09T10:00:00.000Z",
	"meals": [
		{
			"id": "cm4meal001",
			"dayId": "cm4day001",
			"type": "BREAKFAST",
			"name": "Завтрак",
			"mealOrder": 1,
			"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"],
			"createdAt": "2025-12-09T10:00:00.000Z",
			"updatedAt": "2025-12-09T10:00:00.000Z"
		}
	]
}
```

### Ошибки

- **400 Bad Request** - Неверные данные (валидация)
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров
- **404 Not Found** - Подкатегория не найдена или нет прав доступа

---

## Получение планов на день

Получает список всех планов на день в подкатегории с фильтрацией и поиском.

**Endpoint:** `GET /api/nutrition/subcategories/{subcategoryId}/days`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Query параметры

| Параметр | Тип      | Обязательный | По умолчанию | Описание                        |
| -------- | -------- | ------------ | ------------ | ------------------------------- |
| `search` | `string` | Нет          | -            | Поиск по названию дня           |
| `limit`  | `number` | Нет          | 50           | Количество элементов (макс 100) |
| `offset` | `number` | Нет          | 0            | Смещение для пагинации          |

### Успешный ответ (200 OK)

```json
{
	"days": [
		{
			"id": "cm4day001",
			"subcatId": "cm4sub001",
			"dayTitle": "День 1: Набор массы",
			"dayOrder": 1,
			"createdAt": "2025-12-09T10:00:00.000Z",
			"updatedAt": "2025-12-09T10:00:00.000Z",
			"meals": [
				{
					"id": "cm4meal001",
					"dayId": "cm4day001",
					"type": "BREAKFAST",
					"name": "Завтрак",
					"mealOrder": 1,
					"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"],
					"createdAt": "2025-12-09T10:00:00.000Z",
					"updatedAt": "2025-12-09T10:00:00.000Z"
				}
			]
		}
	],
	"pagination": {
		"total": 5,
		"limit": 50,
		"offset": 0,
		"hasMore": false
	}
}
```

### Ошибки

- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров
- **404 Not Found** - Подкатегория не найдена или нет прав доступа

---

## Просмотр конкретного плана

Получает полную информацию о конкретном плане на день, включая приёмы пищи.

**Endpoint:** `GET /api/nutrition/days/{dayId}`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Успешный ответ (200 OK)

```json
{
	"id": "cm4day001",
	"subcatId": "cm4sub001",
	"dayTitle": "День 1: Набор массы",
	"dayOrder": 1,
	"createdAt": "2025-12-09T10:00:00.000Z",
	"updatedAt": "2025-12-09T10:00:00.000Z",
	"meals": [
		{
			"id": "cm4meal001",
			"dayId": "cm4day001",
			"type": "BREAKFAST",
			"name": "Завтрак",
			"mealOrder": 1,
			"items": ["Овсянка с фруктами", "Яйца", "Зеленый чай"],
			"createdAt": "2025-12-09T10:00:00.000Z",
			"updatedAt": "2025-12-09T10:00:00.000Z"
		}
	]
}
```

### Ошибки

- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров
- **404 Not Found** - День не найден или нет прав доступа

---

## Редактирование плана

Частично обновляет план на день. Можно обновить название, порядок или приёмы пищи.

**Endpoint:** `PATCH /api/nutrition/days/{dayId}`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
Content-Type: application/json
```

### Тело запроса (пример обновления названия)

```json
{
	"dayTitle": "День 1: Обновлённый набор массы"
}
```

### Тело запроса (пример обновления приёмов пищи)

```json
{
	"meals": [
		{
			"type": "BREAKFAST",
			"name": "Завтрак обновлённый",
			"mealOrder": 1,
			"items": ["Гречка", "Банан", "Кофе"]
		}
	]
}
```

### Успешный ответ (200 OK)

Аналогично ответу при просмотре конкретного плана.

### Ошибки

- **400 Bad Request** - Неверные данные
- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров
- **404 Not Found** - День не найден или нет прав доступа

---

## Удаление плана

Удаляет план на день с каскадным удалением всех связанных приёмов пищи.

**Endpoint:** `DELETE /api/nutrition/days/{dayId}`

**Права доступа:** `TRAINER`

**Headers:**

```
Authorization: Bearer <access_token>
```

### Успешный ответ (204 No Content)

### Ошибки

- **401 Unauthorized** - Отсутствует или недействителен access token
- **403 Forbidden** - Доступ только для тренеров
- **404 Not Found** - День не найден или нет прав доступа

---

## Модели данных

### NutritionCategory

Категория планов питания, созданная тренером.

```typescript
{
  id: string;                    // CUID
  name: string;                  // Название категории (уникальное)
  description: string | null;    // Описание
  trainerId: string;             // ID тренера-владельца
  subcategories: NutritionSubcategory[]; // Подкатегории
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата обновления
}
```

**Особенности:**

- `name` уникальное в рамках всей системы
- Каждая категория принадлежит конкретному тренеру
- При удалении категории каскадно удаляются все подкатегории, дни и приемы пищи

---

### NutritionSubcategory

Подкатегория в рамках категории питания.

```typescript
{
  id: string;                    // CUID
  name: string;                  // Название подкатегории (уникальное)
  description: string | null;    // Описание
  categoryId: string;            // ID родительской категории
  category: NutritionCategory;   // Связь с категорией
  days: NutritionDay[];          // Дни питания
  plans: ClientNutritionPlan[];  // Назначения клиентам
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата обновления
}
```

**Особенности:**

- `name` уникальное в рамках всей системы
- Принадлежит категории, которая принадлежит тренеру (проверка через `category.trainerId`)
- При удалении каскадно удаляются все дни и приемы пищи

---

### NutritionDay

День питания в рамках подкатегории.

```typescript
{
  id: string;                    // CUID
  subcategoryId: string;         // ID подкатегории
  subcategory: NutritionSubcategory; // Связь с подкатегорией
  dayTitle: string;              // Название дня (например, "День 1")
  dayOrder: number;              // Порядковый номер дня
  meals: NutritionMeal[];        // Приемы пищи
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата обновления
}
```

**Особенности:**

- `dayOrder` определяет последовательность дней в подкатегории
- При удалении каскадно удаляются все приемы пищи

---

### NutritionMeal

Прием пищи в рамках дня.

```typescript
{
  id: string;                    // CUID
  dayId: string;                 // ID дня
  day: NutritionDay;             // Связь с днем
  type: MealType;                // Тип приема пищи (enum)
  name: string;                  // Название приема
  mealOrder: number;             // Порядковый номер в дне
  items: string[];               // Список блюд/продуктов
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата обновления
}
```

**Enum MealType:**

```typescript
enum MealType {
	BREAKFAST = 'BREAKFAST', // Завтрак
	SNACK = 'SNACK', // Перекус
	LUNCH = 'LUNCH', // Обед
	DINNER = 'DINNER', // Ужин
}
```

**Особенности:**

- `items` - массив строк с описанием блюд/продуктов
- `mealOrder` определяет последовательность приемов пищи в течение дня

---

### ClientNutritionPlan

Назначение плана питания клиенту.

```typescript
{
  id: string;                    // CUID
  clientId: string;              // ID клиента
  client: User;                  // Связь с пользователем-клиентом
  subcategoryId: string;         // ID назначенной подкатегории
  subcategory: NutritionSubcategory; // Связь с подкатегорией
  dayIds: string[];              // Массив ID выбранных дней
  createdAt: Date;               // Дата создания
  updatedAt: Date;               // Дата обновления
}
```

**Особенности:**

- `dayIds` - массив ID дней из подкатегории, которые назначены клиенту
- Клиент может иметь несколько назначений (система возвращает последнее)
- При получении плана клиентом фильтруются только дни из `dayIds`

---

## Архитектура и безопасность

### Владение данными

**Категории и подкатегории:**

- Каждая категория принадлежит конкретному тренеру (`trainerId`)
- При создании/изменении/удалении подкатегории проверяется, что `category.trainerId` совпадает с ID авторизованного тренера

**Планы питания:**

- Клиент может просматривать только свои назначения (`clientId` из JWT)
- Только тренер может создавать/изменять дни и приемы пищи

### Уникальные ограничения

- `NutritionCategory.name` - уникальное поле
- `NutritionSubcategory.name` - уникальное поле
- При попытке создать дубликат возвращается `400 Bad Request`

### Каскадное удаление

```
NutritionCategory (удаление)
  └─> NutritionSubcategory (onDelete: Cascade)
       └─> NutritionDay (onDelete: Cascade)
            └─> NutritionMeal (onDelete: Cascade)
```

При удалении категории автоматически удаляются все связанные данные.

---

## Доступ к данным

### Роли и разрешения

**Тренер (TRAINER):**

- ✅ Создает категории и подкатегории
- ✅ Создает дни и приемы пищи (в разработке)
- ✅ Редактирует только свои данные (проверка через `trainerId`)
- ✅ Удаляет свои категории (каскадное удаление)
- ✅ Назначает планы своим клиентам (в разработке)

**Клиент (CLIENT):**

- ✅ Просматривает свой текущий план питания
- ❌ Не может редактировать планы
- ❌ Не видит чужие планы

---

## Статус разработки

**✅ Реализовано (EPIC 6):**

- 6.1 - Prisma модели для системы питания
- 6.2 - CRUD категорий (4 эндпоинта)
- 6.3 - CRUD подкатегорий (4 эндпоинта)

**🔄 В разработке:**

- EPIC 7 - Планы на день (CRUD для NutritionDay и NutritionMeal)
- EPIC 8 - Назначение планов клиентам (ClientNutritionPlan)

---

_Последнее обновление: 5 декабря 2025 г._
