# Seed - Заполнение базы данных тестовыми данными

Этот скрипт автоматически создаёт реалистичные тестовые данные для локальной разработки.

## Использование

```bash
npm run prisma:seed
```

## Облегчённая версия

Если основной seed вызывает ошибки или работает слишком долго, используйте облегчённую версию:

```bash
npm run prisma:seed:light
```

📖 Подробнее об облегчённой версии: [SEED-LIGHT.md](./SEED-LIGHT.md)

## Что создаётся

### Основной seed (prisma:seed)

- **20 тренеров** (trainer1@mail.ru - trainer20@mail.ru)
- **100 клиентов** (client1@mail.ru - client100@mail.ru)
- **~500 прогресс-отчётов** с фотографиями
- **Планы питания** с циклами 7/30 дней
- **Связи тренер-клиент** со статусами и избранным
- **Комментарии** к прогресс-отчётам

### Облегчённый seed (prisma:seed:light)

- **10 тренеров** (trainer1@mail.ru - trainer10@mail.ru)
- **30 клиентов** (client1@mail.ru - client30@mail.ru)
- **~150 прогресс-отчётов** с фотографиями
- **Планы питания** с циклами 7/30 дней
- **Связи тренер-клиент** со статусами и избранным
- **Комментарии** к прогресс-отчётам

**Пароль для всех пользователей:** `123456`

## Структура фото

Все фотографии сохраняются локально в:

- `backend/uploads/photos/trainers/` - фото тренеров
- `backend/uploads/photos/clients/` - фото клиентов
- `backend/uploads/photos/progress/` - фото прогресса

## Особенности

- Все имена, контакты и описания генерируются случайно с помощью `@faker-js/faker`
- Скрипт использует `upsert`, поэтому безопасно запускать несколько раз
- Фото скачиваются с `picsum.photos` (существующие файлы не перезаписываются)
- Прогресс-бар показывает процент выполнения и оставшееся время
- Все пароли захешированы с помощью bcryptjs

## Автоматический запуск

Seed скрипты НЕ запускаются автоматически. После сброса базы данных или применения миграций запустите нужный seed вручную:

```bash
npm run prisma:seed        # Основной seed (20 тренеров, 100 клиентов)
npm run prisma:seed:light  # Облегчённый seed (10 тренеров, 30 клиентов)
```

⚠️ **Сброс базы данных:**

```bash
npx prisma migrate reset --force
```

Эта команда **полностью очистит БД** и применит все миграции. Seed НЕ запустится автоматически — выберите нужную версию и запустите вручную.

**Тестовые аккаунты:**

- Клиенты: `client1@mail.ru` до `client100@mail.ru` (основной) / `client1@mail.ru` до `client30@mail.ru` (облегчённый)
- Тренеры: `trainer1@mail.ru` до `trainer20@mail.ru` (основной) / `trainer1@mail.ru` до `trainer10@mail.ru` (облегчённый)
- Пароль для всех: `123456`
