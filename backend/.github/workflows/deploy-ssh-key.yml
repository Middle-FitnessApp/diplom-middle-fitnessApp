name: Deploy Backend to Timeweb (SSH Key)

on:
  pull_request:
    types: [closed]
    branches:
      - development

jobs:
  deploy:
    # Запускаем только если PR был одобрен и смержен
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/fitness-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Deploy to Timeweb VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.TIMEWEB_HOST }}
          username: root
          key: ${{ secrets.TIMEWEB_SSH_KEY }}
          script: |
            echo "🔄 Pulling latest Docker image..."
            docker pull ${{ secrets.DOCKER_USERNAME }}/fitness-backend:latest
            
            echo "🛑 Stopping old container..."
            docker stop fitness-backend || true
            docker rm fitness-backend || true
            
            echo "🚀 Starting new container..."
            docker run -d \
              --name fitness-backend \
              --restart unless-stopped \
              -p 3000:3000 \
              --env-file /root/fitness-backend.env \
              ${{ secrets.DOCKER_USERNAME }}/fitness-backend:latest
            
            echo "✅ Deployment completed!"
            
            echo "📊 Container status:"
            docker ps | grep fitness-backend
            
            echo "📝 Recent logs:"
            docker logs --tail 20 fitness-backend
