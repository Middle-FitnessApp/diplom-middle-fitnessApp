# Fitness Backend

Backend для проекта онлайн-фитнеса на Fastify + TypeScript + Prisma + PostgreSQL.

## Установка зависимостей

```bash
npm install
```

## Настройка переменных окружения

Скопируйте файл `.env.example` и создайте `.env` со своими значениями:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл, заполнив необходимые значения:

```env
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/fitnessApp"
PORT=3000
JWT_ACCESS_SECRET="ваш-секретный-ключ-для-jwt"
COOKIE_SECRET="ваш-секретный-ключ-для-cookies"
NODE_ENV="development"
FRONTEND_URL="http://localhost:5173"
```

💡 Все необходимые переменные и их описание смотрите в файле `.env.example`

## Настройка базы данных

Убедитесь, что у вас установлен [pgAdmin 4](https://www.pgadmin.org/download/) и запущен PostgreSQL. Создайте базу данных с именем `fitnessApp`.

Для первого запуска проекта (если вы только что клонировали репозиторий):

```bash
npm run prisma:generate       # генерируем клиент Prisma
npm run prisma:migrate        # создаём миграции и применяем их
```

Для последующих запусков проекта (если миграции уже созданы):

```bash
npm run prisma:migrate:dev    # применяет новые миграции
npm run prisma:generate       # обновляет Prisma Client
```

⚠️ Если локальная база содержит конфликтные данные, можно сбросить её:

```bash
npm run prisma:reset          # сбрасывает базу данных и применяет миграции заново
```

## Заполнение базы тестовыми данными

```bash
npm run prisma:seed
```

Это создаст:

- **20 тренеров** с фото и профилями
- **100 клиентов** с фото и профилями
- **~500 прогресс-отчётов** с 3 фото каждый
- Связи тренер-клиент, планы питания, комментарии

**Облегчённая версия (быстрее, меньше данных):**

```bash
npm run prisma:seed:light
```

Это создаст:

- **10 тренеров** с фото и профилями
- **30 клиентов** с фото и профилями
- **~150 прогресс-отчётов** с 3 фото каждый
- Связи тренер-клиент, планы питания, комментарии

📖 Подробнее: [prisma/SEED.md](./prisma/SEED.md) | [prisma/SEED-LIGHT.md](./prisma/SEED-LIGHT.md)

⚠️ **Сброс базы данных:**

```bash
npx prisma migrate reset --force
```

Эта команда **полностью очистит БД** и применит все миграции. Seed НЕ запустится автоматически — выберите нужную версию и запустите вручную:

```bash
npm run prisma:seed        # Основной seed (20 тренеров, 100 клиентов)
npm run prisma:seed:light  # Облегчённый seed (10 тренеров, 30 клиентов)
```

**Тестовые аккаунты:**

- Клиенты: `client1@mail.ru` до `client100@mail.ru` (основной) / `client1@mail.ru` до `client30@mail.ru` (облегчённый)
- Тренеры: `trainer1@mail.ru` до `trainer20@mail.ru` (основной) / `trainer1@mail.ru` до `trainer10@mail.ru` (облегчённый)
- Пароль для всех: `123456`

## Запуск сервера

```bash
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:3000`

---

## Рекомендации для команды

- ⚠️ Не создавайте новую миграцию без необходимости.

Для новых изменений схемы используйте:

```bash
npm run prisma:migrate:name -- <описание>
```

Всегда запускайте `npm run prisma:generate` после изменений схемы.

## Запуск тестов

📄 **[Подробная документация по тестированию](../doc/testing.md)**

Проект использует [Vitest](https://vitest.dev/) для интеграционных тестов.

Коротко — быстрый запуск тестов локально:

- Перейдите в папку `backend` и убедитесь, что есть `backend/.env.test` (или скопируйте `.env.test.example`).
- Запустите тестовую БД (опционально):

```bash
cd backend
docker-compose -f docker-compose.test.yml up -d
```

- Запустите тесты:

```bash
npm test
```

Подробные инструкции и рекомендации по локальному запуску тестов — в [tests/README.md](tests/README.md).
