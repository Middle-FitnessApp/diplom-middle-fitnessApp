<div align="center">

# 📦 Redux Store

### Документация по управлению состоянием FitnessApp

</div>

---

## 📖 Обзор

Store приложения построен на **Redux Toolkit** с использованием **RTK Query** для работы с API. Архитектура разделена на два типа состояния:

| Тип | Описание | Примеры |
|:----|:---------|:--------|
| **Slices** | Локальное состояние приложения | auth, user, ui, chat |
| **API** | Серверные данные с кэшированием | authApi, userApi, nutritionApi... |

---


```
┌─────────────────────────────────────────────────────────────────────────┐
│                              КОМПОНЕНТ                                  │
└────────────────────────────────┬────────────────────────────────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
          ▼                      ▼                      ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   useAuth()     │    │ useGetMeQuery() │    │ dispatch(...)   │
│   useUI()       │    │ RTK Query hooks │    │ actions         │
└────────┬────────┘    └────────┬────────┘    └────────┬────────┘
         │                      │                      │
         ▼                      ▼                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                           REDUX STORE                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │ auth slice  │  │ user slice  │  │  ui slice   │  │ chat slice  │    │
│  └─────────────┘  └─────────────┘  └─────────────┘  └─────────────┘    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐    │
│  │  authApi    │  │   userApi   │  │ nutritionApi│  │ progressApi │    │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘    │
└─────────┼────────────────┼────────────────┼────────────────┼───────────┘
          │                │                │                │
          └────────────────┴────────────────┴────────────────┘
                                    │
                                    ▼
                         ┌─────────────────┐
                         │   Backend API   │
                         └─────────────────┘
```

## 🏗️ Структура папки store

```
store/
├── 📄 index.ts              # Конфигурация store
├── 📄 hooks.ts              # Типизированные хуки
│
├── 📂 slices/               # Локальное состояние
│   ├── auth.slice.ts        # Авторизация
│   ├── user.slice.ts        # Профиль пользователя
│   ├── chat.slice.ts        # Сообщения чата
│   └── ui.slice.ts          # UI состояние
│
├── 📂 api/                  # RTK Query API
│   ├── auth.api.ts          # /auth/*
│   ├── user.api.ts          # /user/*
│   ├── trainer.api.ts       # /trainer/*
│   ├── nutrition.api.ts     # /nutrition/*
│   ├── progress.api.ts      # /progress/*
│   └── chat.api.ts          # /chat/*
│
└── 📂 types/                # TypeScript типы
    ├── auth.types.ts
    ├── user.types.ts
    ├── nutrition.types.ts
    └── progress.types.ts
```